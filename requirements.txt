# Pinned to the environment used for the published runs (Python 3.11.15).
# Install AGAMA separately if the wheel is unavailable for your platform:
#   it needs a C++ toolchain and GSL (on a cluster: module load gsl).

agama==1.0
astropy==8.0.0
astroquery==0.4.11
corner==2.2.3
diptest==0.11.0            # Hartigan's dip test (metallicity unimodality)
emcee==3.1.6
h5py==3.16.0
matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
tqdm==4.68.3               # MCMC progress bars
